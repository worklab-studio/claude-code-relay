// Bundle entry for packages/plugin/dist/mcp.mjs (DESIGN.md §2.3, §9.1).
//
// The MCP agent replaces the body of this file with `import './server.js';` once
// src/server.ts (stdio server, 13 tools, per-call session resolution) exists.
export {};
